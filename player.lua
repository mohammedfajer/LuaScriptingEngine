local player = {}

function player.load()
    -- Initialize player
    print("From player.load()")
end

function player.update(dt)
    -- Update player state
end

function player.draw()
    -- Draw player
end

return player
